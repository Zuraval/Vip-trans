# Подключение EmailJS — отправка форм на почту

## Ключи
- Public Key: `TAc3O3TjZgNtm1Lg6`
- Service ID: `service_ypoqvcl`
- Template ID: `template_vsd28uc`

## Маршрутизация
- Модалка "Оставить заявку" → `service@vip-trans.su`
- Форма расчёта (Авто) → `iz.ekb@vip-trans.su`
- Форма расчёта (ЖД) → `andrey@vip-trans.su`

## Шаги

### 1. Установить пакет
```
npm install @emailjs/browser
```

### 2. Добавить SDK в index.html
```html
<script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
```

### 3. Инициализировать в main.tsx
```tsx
import emailjs from '@emailjs/browser'
emailjs.init('TAc3O3TjZgNtm1Lg6')
```

### 4. ContactModal.tsx — отправка модалки
В `handleSubmit` заменить alert на `emailjs.send(...)` с `to_email: 'service@vip-trans.su'`

### 5. HomePage.tsx — отправка формы расчёта
В `handlePriceSubmit` заменить alert на `emailjs.send(...)` с:
- `to_email: transportType.includes('Авто') ? 'iz.ekb@vip-trans.su' : 'andrey@vip-trans.su'`

### 6. Шаблон в EmailJS
В поле "To Email" шаблона: `{{to_email}}`

## Верификация
1. Модалка → письмо на service@vip-trans.su
2. Форма (Авто) → iz.ekb@vip-trans.su
3. Форма (ЖД) → andrey@vip-trans.su
